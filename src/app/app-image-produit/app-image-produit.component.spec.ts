/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AppImageProduitComponent } from './app-image-produit.component';

describe('Component: AppImageProduit', () => {
  it('should create an instance', () => {
    let component = new AppImageProduitComponent();
    expect(component).toBeTruthy();
  });
});
